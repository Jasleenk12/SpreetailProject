# Database
# Local dev (SQLite):
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL — switch prisma schema provider to "postgresql" and uncomment):
# DATABASE_URL="postgresql://user:password@host:5432/splitwise_clone?schema=public"

JWT_SECRET="your-super-secret-jwt-key-change-in-production"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
