# CSV Import Report

**Group:** Apartment 4B  
**Generated:** 2026-06-13T18:00:00.000Z  
**Source file:** `data/sample-expenses.csv`  
**Produced by:** SplitEase app (`POST /api/groups/:id/import`)

> This is a sample report generated by importing `data/sample-expenses.csv` into the demo group.  
> You can reproduce it: log in as Alice → open "Apartment 4B" → click **Import CSV** → upload `data/sample-expenses.csv`.

---

## Summary

| Metric | Count |
|--------|-------|
| Rows imported | 5 |
| Rows skipped | 5 |
| Rows auto-fixed | 1 |
| Total anomalies | 6 |

---

## Anomalies Detected

| Row | Field | Issue | Raw Value | Action | Detail |
|-----|-------|-------|-----------|--------|--------|
| 5 | amount | Negative or zero amount | -30 | skipped | — |
| 6 | description | Missing description | (empty) | skipped | — |
| 7 | paid_by_email | Payer not found in group | unknown@example.com | skipped | — |
| 8 | participant_values | Percentages sum to 90, not 100 | 50;30;10 | skipped | — |
| 9 | expense_date | Invalid date format | not-a-date | fixed | Defaulted to today's date |
| 10 | amount | Non-numeric or missing amount | N/A | skipped | — |

---

## Successfully Imported Rows

| Row | Description | Amount | Paid By | Split Type |
|-----|-------------|--------|---------|------------|
| 2 | Groceries | $90.00 | alice@example.com | EQUAL |
| 3 | Electricity Bill | $120.00 | bob@example.com | PERCENTAGE |
| 4 | Dinner | $45.50 | charlie@example.com | EQUAL |
| 9 | Cleaning supplies | $25.99 | bob@example.com | EXACT (date auto-fixed) |
| 11 | Movie tickets | $36.00 | alice@example.com | SHARES |

---

## Notes

- Rows 2–4 were valid and imported without anomalies (rows 2–3 may already exist from seed data and will appear as duplicates in the group — this is expected behavior; the import does not deduplicate).
- Row 9 (`Cleaning supplies`) had an invalid date but was imported after defaulting `expense_date` to today.
- Row 5 (`Internet`, amount `-30`) was rejected — negative amounts are not allowed.
- Row 6 had no description — required field, skipped.
- Row 7 referenced `unknown@example.com` who is not a group member — skipped.
- Row 8 (`Pizza`) had percentages totaling 90% instead of 100% — skipped.
- Row 10 had amount `N/A` — non-numeric, skipped.

---

## How to Generate a Fresh Report

1. Start the app: `npm run dev`
2. Log in: `alice@example.com` / `password123`
3. Open group **Apartment 4B**
4. Click **Import CSV** and select `data/sample-expenses.csv`
5. The import report appears inline on the group page
6. The API also returns the report as markdown in the JSON response field `report`
